version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: TestTask
        LoadBalancerInfo:
          ContainerName: 'TestContainer'
          ContainerPort: 80
